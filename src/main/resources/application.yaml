server:
  port: 8082

spring:
  application:
    name: plan-voyage
  datasource:
    url: jdbc:mysql://localhost:3306/plan_voyage
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

